class Solution {
    public int[][] merge(int[][] intervals) {
        int n=intervals.length;
        Arrays.sort(intervals,(c,d)->c[0]-d[0]);

        List<int []> ans=new ArrayList<>();
        for(int []arr:intervals){
            if(ans.isEmpty() || ans.get(ans.size()-1)[1]< arr[0]){
                ans.add(arr);
            }
            else{
                
                ans.get(ans.size()-1)[1]=Math.max(ans.get(ans.size()-1)[1],arr[1]);
            }
        }
        return ans.toArray(new int [ans.size()][]);
    }
}




-------if ans is empty mean not any element prsent and other case when the end of last elemnt prent in the list is smaller than the first elemnt of th
arry the we have to add in the list because overlapping is not present there
if this is not the case the defnitely ther overlapping but here is the catch if the last element of exixting interval in the list is gresater than equal to 
the new interval then we need previse one only thats why uswed maximum so that it handle this case also like[2,5][3,4],so her we have to keep that 5 only
